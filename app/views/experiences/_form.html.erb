<%= form_with(model: experience) do |form| %>
  <% if experience.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(experience.errors.count, "error") %> prohibited this experience from being saved:</h2>

      <ul>
        <% experience.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :user, Current.user %>

  <div class="form-group row">
    <%= form.label :project_name %>
    <%= form.text_field :project_name , class: "form-control" %>
  </div>

  <div class="form-group row">
    <%= form.label :from %>
    <%= form.date_select :from  , class: "form-control"%>
  </div>

  <div class="form-group row">
    <%= form.label :to %>
    <%= form.date_select :to , class: "form-control" %>
  </div>

  <div class="form-group row">
    <%= form.label :description %>
    <%= form.text_field :description, class: "form-control" %>
  </div>

  <div class="form-group row">
    <%= form.submit  class: "btn btn-primary" %>
  </div>
<% end %>
